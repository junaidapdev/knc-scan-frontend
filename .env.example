VITE_API_BASE_URL=http://localhost:3000
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_SENTRY_DSN=
VITE_POSTHOG_KEY=
# Microsoft Clarity project id (session recordings + heatmaps). Leave empty to
# disable. Set ONLY in the Vercel production environment so dev/preview don't
# record. Customer PWA only — the admin console is never recorded.
VITE_CLARITY_PROJECT_ID=
VITE_APP_NAME=Kayan Sweets
